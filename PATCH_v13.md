# v13 · 디스코드 계산기

## 추가 기능

- `/계산기` 슬래시 명령을 추가했습니다.
- 공격 포켓몬, 방어 포켓몬과 기술은 Discord 자동완성으로 선택합니다.
- 기술 후보는 선택한 공격 포켓몬의 현재 모드 통계에 등재된 공격기만 표시합니다.
- 상대 내구 기준은 `H0 · B/D0`, `H32 · B/D0`, `H32 · B32`, `H32 · D32` 중에서 선택합니다.
- 결과에는 86~100의 15난수 데미지, 최소·최대 비율, 확정/난수 타수, 자속, 타입상성, 도구·특성 보정, 결정력 지수, 물리·특수 내구력과 스피드를 표시합니다.
- 공격 측은 해당 모드의 도구·특성·성격·능력배분 채용률 1위를 조합한 참고 세팅을 사용하며, 실제 동시 채용 세트라고 표시하지 않습니다.

## 배포 후 필요한 작업

Lambda 코드는 웹앱 배포 명령으로 함께 갱신됩니다. 새 슬래시 명령은 Discord API에도 한 번 다시 등록해야 합니다.

```bash
export DISCORD_APPLICATION_ID=1546498132655874068
export DISCORD_GUILD_IDS=1330195389839704215,1525552172417286266
read -s DISCORD_BOT_TOKEN
export DISCORD_BOT_TOKEN
node discord/register.mjs
unset DISCORD_BOT_TOKEN
```

토큰은 화면에 표시되지 않는 `read -s`로 입력하고 채팅이나 파일에 저장하지 않습니다.
