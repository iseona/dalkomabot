# Calculator and shared UI

Scope: calculator controller, shared templates, escaping, and toast behavior. Keep battle formulas in `dist/engine.mjs`; keep DOM wiring in `dist/calculator.mjs`. Run `node tests/verify.mjs`.
