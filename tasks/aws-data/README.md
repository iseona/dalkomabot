# AWS and data

Scope: CloudFormation, deploy tooling, the validated data collector, and open-data import. Scheduled collection remains disabled by default. Recognition has a hard daily request quota. Never place `OPENAI_API_KEY` in CloudFormation parameters or files.
