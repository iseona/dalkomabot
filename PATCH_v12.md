# v12 — 포챔스 달콤아 봇 브랜딩과 235종 이미지 DB

## 로토무 폼 처리

`로토무`, `워시로토무`, `히트로토무`, `커트로토무`, `프로스트로토무`, `스핀로토무`를 서로 다른 후보로 유지합니다. 이전 샘플의 두 번째 내 파티 슬롯만 `워시로토무`가 정답이며, 다른 로토무 폼을 워시로토무로 합치지 않습니다.

## 이미지 인식 DB

- 현재 싱글·더블 리스팅 235종 모두에 폼별 기본 이미지 참조를 등록했습니다.
- 사용자가 제공한 챔피언스 선출 화면 세 장에서 확인 가능한 36개 화면 조각을 별도 정밀 참조로 추가했습니다.
- 2·3번 극저화질 이미지는 일반적인 기준 이미지를 오염시키지 않도록 학습 DB에는 넣지 않았습니다.
- 03 스크린샷 입력과 05 선출 추천이 동일한 DB를 사용합니다.
- 고해상도 일반색·이로치 렌더가 배포되어 있으면 추가 보조 참조로 사용합니다.
- 사용자가 확정한 화면 특징은 해당 브라우저에 누적됩니다.
- 시즌 데이터와 이미지 목록을 갱신할 때 배포 스크립트가 현재 리스팅에 맞춰 기본 이미지 DB를 다시 만들고, 아직 유효한 챔피언스 화면 조각만 보존합니다.

## 브랜딩

- 웹앱 이름을 `포챔스 달콤아 봇`으로 변경했습니다.
- 헤더, 브라우저 제목, 파비콘, 홈 화면 아이콘, 설치형 웹앱 매니페스트에 달콤아 이미지를 적용했습니다.
- 디스코드 명령어 안내 제목도 `포챔스 달콤아 봇`으로 통일했습니다.
- Discord Developer Portal 앱 아이콘으로 사용할 512px 이미지를 포함했습니다.

## 검증

- 235종 기본 이미지가 모두 존재하고 이름이 중복되지 않는지 검사
- 로토무 6폼이 각각 별도 참조로 존재하는지 검사
- 챔피언스 화면 조각 36개와 포켓몬 이름의 현재 목록 포함 여부 검사
- 브랜딩용 이미지와 웹앱 매니페스트 검사
- 앱 JavaScript 문법 및 기존 기능 자동 테스트
