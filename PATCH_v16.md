# v16 이미지 인식 — WIP

> 2026-09-09: 브라우저 OCR을 제거하고 서버 측 OpenAI 이미지 인식으로 교체했다. 아래 OCR 내용은 이전 WIP 기록이다. 실제 API 샘플 검증 전까지 main 병합과 AWS 배포는 금지한다.

작업 브랜치: `feature/v16-recognition`

OCR 샘플 검증을 통과하기 전 main 병합 및 AWS 배포 금지.

## 이번 수정

- 스탯 화면을 슬롯 전체 문자 OCR에만 맡기지 않고, 고정된 2열 3행 카드의 숫자 영역을 별도로 잘라 640×240 흑백 이미지로 확대.
- 숫자 전용 OCR은 문자 화이트리스트를 `0~9`로 제한하고, 여섯 배분이 모두 0~32이며 합계 66 이하일 때만 전체 화면 OCR보다 우선 적용.
- 스탯 파싱·숫자 전처리와 이미지 특징 생성·후보 점수·중복 후보 배정을 `dist/recognition.mjs`로 분리해 UI 없이 단위 검증 가능.
- 선출 및 계산기 HTML 문자열을 `dist/ui-templates.mjs`로 분리해 앱 로직 검색에 마크업 전체가 섞이지 않도록 정리.
- 계산기 이벤트·결정력·스피드 결과 렌더링을 `dist/calculator.mjs`, HTML 이스케이프와 토스트를 `dist/ui.mjs`로 분리.
- Windows CRLF 체크아웃에서도 기존 검증 실행 가능.
- 단일 후보, 동점·근접 후보, 비정상 점수는 이미지 인식 자동 확정에서 제외.
- 비어 있는 이미지 특징과 손상된 브라우저 학습 특징을 비교에서 제외.
- 화면 샘플 등록 시 v16 schemaVersion 3 유지. DB 읽기 오류는 즉시 중단하며 기존 DB를 빈 데이터로 덮어쓰지 않음.
- 샘플의 종 이름, 양쪽 중복, 좌표 순서 및 영역 폭 검증 추가.

## 검증 결과

`node tests/verify.mjs` 통과: 기존 통계·OCR 숫자 파서·숫자 전용 우선 적용·Discord 검증과 이미지 특징/확정 경계 회귀 검사.

`node --check dist/app.js`, `node --check scripts/add_recognition_sample.mjs`, `git diff --check` 통과.

이 결과는 실제 이미지 OCR 샘플 검증 통과를 뜻하지 않는다. 샘플 등록 스크립트의 실제 이미지 변환 실행은 미검증이다.

## 남은 샘플 검증

저장소에 선출 아이콘 조각 36개는 있으나 전체 원본 화면 및 슬롯별 기대 정답 fixture는 없다. 실제 한국어/영어 세팅·스테이터스·선출 화면과 정답을 확보한 뒤 다음을 확인해야 한다.

- 실제 브라우저에서 로컬 Tesseract 엔진으로 업로드부터 후보 생성·수정·적용까지 실행.
- 슬롯별 이름, 도구, 특성, 기술, 36개 배분 값을 정답과 비교. 통계 보완 값을 OCR 정답으로 집계하지 않음.
- 저화질·빈 화면·미지원 종은 잘못 확정하지 않고 수동 확인으로 남는지 확인.
- 평가 화면을 학습 DB에 등록하지 않은 상태로 먼저 측정하고 결과를 기록.

현재 상태: **실제 OCR 샘플 검증 대기 / WIP 유지**.

## 구조 리팩터링 우선순위

1. `recognition.mjs`: 스탯 전처리·파싱과 이미지 특징·후보 계산(완료)
2. `ui-templates.mjs`: 선출·계산기 정적 마크업 분리(완료)
3. `calculator.mjs`, `ui.mjs`: 계산기 제어와 공통 UI 헬퍼 분리(완료)
4. 선출 화면 제어와 선출 조합 계산 분리

기능을 옮길 때마다 `tests/verify.mjs`를 통과시켜 인식 변경과 구조 변경의 원인을 분리한다.
