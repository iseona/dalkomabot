# v14 · 빠른 계산기와 배포 캐시 보강

## Discord

- `/결정력계산기`: 공격·방어 포켓몬만 선택하면 공격 측 통계 상위 4개 기술 중 대상에게 가장 큰 피해를 주는 기술을 고릅니다.
- `H0 B0`, `H32 B0`, `H32 B32`, `H0 B32`, `H32 D0`, `H32 D32`, `H0 D32`의 데미지 범위와 확정·난수 타수를 줄별로 표시합니다.
- `/스피드계산기`: 두 포켓몬의 최속, 준속, 무보정과 각 구애스카프 기준을 줄별로 비교합니다.
- 기존 `/계산기`는 기술과 방어 배분을 직접 고르는 상세 계산기로 유지합니다.

## 웹앱

- 계산기에서 공격·방어 포켓몬만 고른 뒤 `결정력 일괄 계산` 또는 `스피드 일괄 비교`를 바로 실행할 수 있습니다.
- 기존의 기술·방어 배분 상세 계산도 함께 유지합니다.

## 파비콘·앱 이름 캐시

- 파비콘, Apple 아이콘과 manifest 주소에 v14 버전을 부여했습니다.
- `index.html`과 `manifest.webmanifest`는 S3에서 `no-cache`로 다시 업로드합니다.
- 배포 스크립트가 CloudFront 무효화 완료까지 기다린 뒤 `CloudFront cache refresh completed.`를 출력합니다. 이 문구가 나온 후 새 버전 확인을 시작하면 됩니다.

## 계산 범위

- 챔피언스의 능력 배분 상한(개별 32, 총합 66)과 86~100의 15난수를 사용합니다.
- 조건부 위력, 연속기 횟수, 가변 위력, 날씨·필드·랭크·벽 등 현재 데이터로 확정할 수 없는 효과는 제외하고 결과에 표시합니다.
