# v11 — 공용 화면 이미지 인식 DB

## 바뀐 점

- `05 선출 추천`과 `03 스크린샷 입력`이 같은 이미지 특징 DB와 현재 배틀 모드의 포켓몬 목록을 사용합니다.
- 제공받은 실제 챔피언스 선출 화면 1장을 내 파티 6칸·상대 파티 6칸의 기준 이미지로 분리해 `dist/recognition-db.json`에 등록했습니다.
- 기준 정답은 내 파티 `한카리아스, 워시로토무, 마스카나, 리자몽, 나인테일, 메타그로스`, 상대 파티 `하마돈, 한카리아스, 마스카나, 왕구리, 브리두라스, 찌리배리`입니다.
- 포켓몬이 패널 가장자리에서 잘리는 경우를 위해 각 칸을 좁게·보통·넓게 세 번 비교합니다.
- 배경 패널과 광선 색상을 최대한 제거하고 색상보다 실루엣·형태에 높은 가중치를 둡니다.
- 타입색 추정은 포켓몬 후보 순위에 반영하지 않으며 신뢰도가 낮으면 표시하지 않습니다.
- 같은 포켓몬 하나가 여섯 칸의 1순위를 독점하지 않도록 한 화면 안의 후보를 함께 재정렬합니다. 내 파티와 상대 파티 사이의 같은 포켓몬은 허용합니다.
- 사용자가 12칸을 교정한 뒤 `확정한 화면을 이 기기 인식 DB에 저장`을 누르거나, 03에서 결과를 적용하면 해당 화면 특징이 브라우저에 누적됩니다.

## 이미지 자료 범위

현재 배포본에는 제공받은 화면에서 확인된 10종, 12개 화면 조각이 정밀 기준으로 들어 있습니다. 나머지 현재 목록은 기존 PokeAPI 일반·이로치 렌더를 보조 기준으로 사용합니다. `champs.pokedb.tokyo`의 전체 이미지를 자동 수집하지는 않습니다.

새 시즌이나 레귤레이션에서는 먼저 챔피언스 전용 포켓몬·도구·특성·기술 목록을 갱신하고, 실제 선출 화면에서 사용자가 확정한 조각을 추가하는 방식으로 기준 DB를 누적합니다. 이전 시즌 자료는 시즌 키를 분리해 현재 후보군에 섞이지 않게 유지해야 합니다.

## 검증

- 앱 JavaScript 문법 검사 통과
- 전체 자동 테스트 통과
- 기준 이미지 12개와 공식 한글 이름 매핑 검사 통과
- 싱글·더블 현재 후보 235종 제한 검사 통과

